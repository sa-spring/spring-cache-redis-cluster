## Spring Caching with Redis Cluster

This project demonstrates Spring Caching with Redis Cluster.

> h2 DB console is run at `http://localhost:8080/h2-console` and change the JDBC URL to `jdbc:h2:mem:testdb`.